CPU_LOAD 1.0

Programma per la monitorizzazione (sia locale, sia remota) di CPU

Viene mostrata a video il carico medio di tutta la CPU e nel dettaglio il carico di ogni singolo Core (Sia reale sia virtuale) con anche l'utilizzo di un grafico

È possibile utilizzarlo anche da remoto tramite la libreria Pyro4. Basta inserire l'indirizzo IP nel programma e se 

Dipendenza

Libreria psutils
https://code.google.com/p/psutil/
